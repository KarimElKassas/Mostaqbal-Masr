import 'package:flutter/material.dart';

class CustomerQuestionsScreen extends StatelessWidget {
  const CustomerQuestionsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
